import type { InputHTMLAttributes } from "react";
export function Input({ label,error,className="",id,...props }: InputHTMLAttributes<HTMLInputElement>&{label?:string;error?:string}) { const inputId=id??props.name; return <label className="block space-y-2" htmlFor={inputId}><span className="font-mono text-xs uppercase tracking-[.12em] text-ink-soft">{label}</span><input id={inputId} className={`w-full rounded-xl border border-paper-deep bg-white/60 px-4 py-3 text-ink placeholder:text-ink-soft/60 focus:border-coral focus:bg-white focus:outline-none ${className}`} {...props}/>{error&&<span className="text-sm text-coral-dark">{error}</span>}</label>; }
