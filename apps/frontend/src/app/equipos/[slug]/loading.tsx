import { LoadingState } from "@/components/feedback/FeedbackState";

export default function Loading() {
  return <LoadingState title="Actualizando el marcador" description="Estamos reuniendo los datos del equipo." />;
}
