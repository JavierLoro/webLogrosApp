generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Logro {
  id     Int    @id @default(autoincrement())
  nombre String
  puntos Int
}

model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
}
